# ========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ========================================
#
# WICHTIG: Kopiere diese Datei zu .env auf dem Production-Server
#          und fülle ALLE Werte mit echten Production-Keys!
#
# NIEMALS Development/Test-Keys in Production verwenden!
# ========================================

# ==================== DATABASE ====================
# Production MongoDB URI (Atlas, etc.)
# ⚠️ NICHT die Test/Dev-Datenbank verwenden!
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# ==================== SECURITY ====================
# JWT Secret für Token-Signierung (mindestens 32 Zeichen)
JWT_SECRET=<secure-random-string-min-32-chars>

# Encryption Key für Contract Artifacts (GENAU 64 Zeichen!)
# ⚠️ KRITISCH: Muss identisch mit ENCRYPTION_SECRET_KEY sein!
# Generieren mit: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=<64-char-hex-string>
ENCRYPTION_SECRET_KEY=<same-64-char-hex-string>

# ==================== OPENAI API ====================
# Production OpenAI API Key mit ausreichend Budget!
# ⚠️ Budget überwachen: ~$0.01-0.05 pro Contract-Generierung
OPENAI_API_KEY=sk-proj-...

# ==================== AWS S3 ====================
# Production AWS Credentials für PDF-Storage
AWS_ACCESS_KEY_ID=<production-aws-key>
AWS_SECRET_ACCESS_KEY=<production-aws-secret>
AWS_REGION=eu-north-1
S3_BUCKET_NAME=<production-bucket-name>

# ==================== STRIPE PAYMENTS ====================
# Production Stripe Keys (NICHT Test-Mode!)
# ⚠️ Test-Keys beginnen mit sk_test_, Production mit sk_live_
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ==================== EMAIL SERVICE ====================
# Production Email Server für Verifizierung & Notifications
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=<production-email@domain.com>
EMAIL_PASS=<email-app-password>

# ==================== OPTIONAL ====================
# Node Environment (immer 'production' auf Live-Server!)
NODE_ENV=production

# Server Port (Standard: 5000)
PORT=5000

# ==================== MONITORING (EMPFOHLEN) ====================
# Sentry DSN für Error Tracking (optional aber empfohlen)
# SENTRY_DSN=https://...@sentry.io/...

# ==================== NOTES ====================
#
# DEPLOYMENT CHECKLIST:
# 1. ✅ Alle <placeholders> mit echten Werten ersetzen
# 2. ✅ ENCRYPTION_KEY mit: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" generieren
# 3. ✅ Stripe Keys auf Production umstellen (sk_live_)
# 4. ✅ MongoDB URI auf Production-Cluster ändern
# 5. ✅ OpenAI Budget prüfen (mindestens $50 empfohlen)
# 6. ✅ AWS S3 Bucket erstellen und Permissions setzen
# 7. ✅ Email SMTP testen (Test-Email senden)
# 8. ✅ Datei umbenennen zu .env (ohne .template)
# 9. ✅ Niemals .env in Git committen! (.gitignore prüfen)
#
# SECURITY:
# - Alle Keys regelmäßig rotieren (mind. alle 6 Monate)
# - Backup der .env an sicherem Ort (nicht in Git!)
# - Bei Key-Leak sofort alle Keys invalidieren und neu generieren
#
# ========================================
